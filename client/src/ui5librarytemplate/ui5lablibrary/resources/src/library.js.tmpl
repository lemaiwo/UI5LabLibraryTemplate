sap.ui.define(['jquery.sap.global', 'sap/ui/core/library'],
    function(jQuery, library1) {
        "use strict";

        sap.ui.getCore().initLibrary({
            name : "{{ui5lab.parameters.namespace.value}}",
            dependencies : ["sap.ui.core"],
            types: [],
            interfaces: [],
            controls: [
                "{{ui5lab.parameters.namespace.value}}.{{ui5lab.parameters.name.value}}"
            ],
            elements: [],
            noLibraryCSS: false,
            version: "${version}"
        });
    return {{ui5lab.parameters.namespace.value}};
});